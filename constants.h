#include <iostream>

// Where the wins history will be permanently stored
std::string HISTORY_PATH = "history.txt";

// Where the wins history will be temporarily stored
std::string HISTORY_BUFFER_PATH = "history_.txt";